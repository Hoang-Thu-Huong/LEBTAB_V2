{
  "name": "lebtab-v2-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "LEBTAB_V2 Eingabemaske — Express-Backend + statisches Frontend (docs/SPEC.md, DATA.md, ARCHITECTURE.md)",
  "engines": {
    "node": ">=18.11"
  },
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "vitest run"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.1.0",
    "multer": "^2.0.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "supertest": "^7.0.0",
    "vitest": "^3.0.0"
  }
}
